# Este programa cuenta las líneas de un archivo SAM (introducido como argumento) y devuelve:
# 1. Aquellas que tienen MAPQ = 60.
# 2. Total de líneas (secuencias).
# 3. Porcentaje de secuencias con MAPQ = 60.
# Para mostrarlo utiliza el paquete rich, por lo que se recomienda, en caso de no tenerlo instalado, utilizar UV.
# También se proporciona archivo nf para utilizar con nextflow.
